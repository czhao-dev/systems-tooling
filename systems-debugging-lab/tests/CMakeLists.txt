foreach(test_name test_parser test_aggregator test_query_engine test_thread_pool)
    add_executable(${test_name} ${test_name}.cpp)
    target_link_libraries(${test_name} PRIVATE logforge_core)
    add_test(NAME ${test_name} COMMAND ${test_name})
endforeach()
